#
# AI Service Environment Template (test)
#

# === OpenAI Credentials ===
# Your OpenAI API key used for embeddings, matching, and feedback requests.
OPENAI_API_KEY=sk-test-your-key-here

# === Embedding Generation ===
# Maximum number of texts sent per embedding request.
EMBEDDING_MAX_BATCH=16
# TTL (seconds) for cached embeddings; reduce for deterministic tests.
EMBEDDING_CACHE_TTL_SECONDS=60
# Maximum cached embeddings retained in-memory.
EMBEDDING_CACHE_MAX_ENTRIES=128
# Maximum character length allowed after preprocessing.
EMBEDDING_MAX_INPUT_CHARS=8000

# === Matching Algorithm ===
# Minimum combined score required for a match to be returned.
MATCH_MIN_SCORE=0.25
# Maximum matches returned per request.
MATCH_MAX_RESULTS=25
# TTL (seconds) for cached match responses.
MATCH_CACHE_TTL_SECONDS=120
# Maximum cached match responses retained.
MATCH_CACHE_MAX_ENTRIES=256
# Half-life in days for freshness decay (job recency).
MATCH_FRESHNESS_HALF_LIFE_DAYS=14
# Optional weighting overrides (set blank to use defaults).
MATCH_WEIGHT_EMBEDDING=0.6
MATCH_WEIGHT_SKILLS=0.2
MATCH_WEIGHT_EDUCATION=0.1
MATCH_WEIGHT_EXPERIENCE=0.05
MATCH_WEIGHT_FRESHNESS=0.05

# === Feedback Generation ===
# OpenAI model for feedback responses.
FEEDBACK_MODEL=gpt-4.1-mini
# Sampling temperature for feedback responses.
FEEDBACK_TEMPERATURE=0.4
# Maximum tokens permitted in the feedback response.
FEEDBACK_MAX_TOKENS=900
# Default language (ISO 639-1 code) when none supplied.
FEEDBACK_DEFAULT_LANGUAGE=en

# === Assessment Question Generation ===
# OpenAI model used to generate assessment questions.
ASSESSMENT_MODEL=gpt-4.1-mini
# Sampling temperature for question generation.
ASSESSMENT_TEMPERATURE=0.3
# Maximum tokens allocated per question generation response.
ASSESSMENT_MAX_TOKENS=1000

# === Service Behaviour ===
# FastAPI host/port overrides for local testing.
APP_HOST=0.0.0.0
APP_PORT=8000

